spring:
  cloud:
    config:
      uri: http://localhost:8888
  datasource:
    username: root
    password: strongpassword
  application:
    name: order-service

management:
  endpoints:
    web:
      exposure:
        include: '*'

server:
  port: 0

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}

